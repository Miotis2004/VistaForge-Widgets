<UserControl
    x:Class="VistaForge.Widgets.NotesWidget.NotesWidgetControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VistaForge.Widgets.NotesWidget"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="LightYellow" Width="250" Height="250" CornerRadius="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Notes" Grid.Row="0" FontWeight="Bold" Margin="8,8,8,0"/>

        <RichEditBox x:Name="NotesEditBox" Grid.Row="1" Margin="8"
                     Background="Transparent" BorderThickness="0"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     TextChanged="NotesEditBox_TextChanged"/>
    </Grid>
</UserControl>
